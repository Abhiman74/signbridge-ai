# lib/mediapipe

Reserved for the MediaPipe Tasks Vision integration (Milestone 2): loading
the Hand Landmarker / Holistic tasks, running them against camera frames,
and normalizing landmark output into the `FrameLandmarks` shape defined in
`types/index.ts`.
